#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{   
    
    if (argc < 2 || argc > 2)
    {
        printf("Usage: ./caesar key\n");
        return 1;
    }      
    string input = argv[1];
    for (i = 0, n = strlen(input); i < n; i++);
    {
        printf("%c", input
    }
    // Print checks:
    //printf("%i\n", inputCheck);
    printf("%s\n", argv[1]);
}
